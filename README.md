# Task Manager - Flutter приложение

Мобильное приложение для управления задачами, созданное на Flutter. Приложение поддерживает полный набор CRUD операций (создание, чтение, обновление, удаление) и работает на всех платформах, поддерживаемых Flutter.

## Функциональность

- 📝 Добавление новых задач через текстовое поле
- ✅ Отметка выполнения задач (чекбокс с зачеркиванием)
- ✏️ Редактирование существующих задач
- 🗑️ Удаление задач
- 📱 Адаптивный интерфейс для мобильных устройств
- 🌐 Поддержка русского языка интерфейса

## Технологии

- **Flutter 3.38.3** - фреймворк для кроссплатформенной разработки
- **Dart 3.10.1** - язык программирования
- **Material Design 3** - система дизайна

## Структура проекта

```
task_manager_app/
├── lib/
│   └── main.dart          # Основной файл приложения
├── test/
│   └── widget_test.dart   # Виджет-тесты
├── README.md              # Этот файл
└── pubspec.yaml           # Зависимости и конфигурация
```

## Запуск приложения

### Предварительные требования

1. Установите [Flutter SDK](https://flutter.dev/docs/get-started/install)
2. Убедитесь, что Flutter правильно настроен:
   ```bash
   flutter doctor
   ```

### Запуск на разных устройствах

#### 1. Веб-браузер (Chrome/Edge)
```bash
cd task_manager_app
flutter run -d chrome
```
или
```bash
cd task_manager_app
flutter run -d edge
```

Приложение будет доступно по адресу: `http://localhost:8080`

#### 2. Windows (десктоп)
```bash
cd task_manager_app
flutter run -d windows
```

#### 3. Android эмулятор
1. Запустите Android Studio и создайте эмулятор
2. Убедитесь, что эмулятор запущен:
   ```bash
   flutter devices
   ```
3. Запустите приложение:
   ```bash
   cd task_manager_app
   flutter run -d emulator-5554
   ```

#### 4. iOS симулятор (только на macOS)
```bash
cd task_manager_app
flutter run -d iPhone
```

#### 5. Физическое устройство
1. Подключите устройство через USB
2. Включите режим разработчика
3. Запустите приложение:
   ```bash
   cd task_manager_app
   flutter run -d <device_id>
   ```

### Сборка приложения

#### Веб-сборка
```bash
cd task_manager_app
flutter build web
```
Собранные файлы будут в папке `build/web`

#### Сборка для Android
```bash
cd task_manager_app
flutter build apk
```
или для app bundle:
```bash
flutter build appbundle
```

#### Сборка для iOS
```bash
cd task_manager_app
flutter build ios
```

#### Сборка для Windows
```bash
cd task_manager_app
flutter build windows
```

## Тестирование

Запуск тестов:
```bash
cd task_manager_app
flutter test
```

## Структура кода

### Основные компоненты

1. **TaskManagerApp** - корневой виджет приложения
2. **TaskManagerHomePage** - главная страница (StatefulWidget)
3. **Task** - модель данных задачи
4. **ListView.builder** - для эффективного отображения списка задач
5. **TextField** - для ввода новых задач и редактирования

### Ключевые методы

- `_addTask()` - добавление новой задачи
- `_deleteTask()` - удаление задачи
- `_startEditing()` - начало редактирования
- `_saveEdit()` - сохранение изменений
- `_toggleTaskCompletion()` - переключение статуса выполнения

## Скриншоты

(Добавьте скриншоты вашего приложения здесь)

## Лицензия

MIT
